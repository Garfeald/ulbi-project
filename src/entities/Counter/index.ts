import { Counter } from './ui/Counter';
import { counterReducer } from './model/slice/counterSlice';
import { CounterScheme } from './model/types/counterScheme/counterScheme';

export {
    counterReducer,
    CounterScheme,
    Counter,
};
